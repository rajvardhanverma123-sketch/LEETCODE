int sum = 0, count = 0;
        std::unordered_map<int, int> freq = {{0, 1}};
        for (int num : nums) {
            sum += num;
            count += freq[sum - k];
            freq[sum]++;
        }
        return count;
